const { asyncHandler } = require('../../utils/asyncHandler'); const { success, created } = require('../../utils/response'); const service = require('./service'); const list = asyncHandler(async (req, res) => success(res, await service.list(req.query, req.user))); const create = asyncHandler(async (req, res) => created(res, { message: 'Đã gửi đánh giá', data: await service.create(req.user, req.body) })); const update = asyncHandler(async (req, res) => success(res, { message: 'Đã cập nhật đánh giá', data: await service.update(req.user, req.params.id, req.body) })); const moderate = asyncHandler(async (req, res) => success(res, { message: 'Đã kiểm duyệt đánh giá', data: await service.moderate(req.user, req.params.id, req.body) })); const remove = asyncHandler(async (req, res) => success(res, { message: 'Đã ẩn đánh giá', data: await service.remove(req.user, req.params.id) })); module.exports = { list, create, update, moderate, remove };
