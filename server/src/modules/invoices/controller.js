const { asyncHandler } = require('../../utils/asyncHandler'); const { success, created } = require('../../utils/response'); const service = require('./service'); const create = asyncHandler(async (req, res) => created(res, { message: 'Đã lập hóa đơn', data: await service.create(req.user, req.body) })); const list = asyncHandler(async (req, res) => success(res, await service.list(req.user, req.query))); const show = asyncHandler(async (req, res) => success(res, { data: await service.getForUser(req.user, req.params.id) })); const update = asyncHandler(async (req, res) => success(res, { message: 'Đã cập nhật hóa đơn', data: await service.updateStatus(req.user, req.params.id, req.body) })); module.exports = { create, list, show, update };
