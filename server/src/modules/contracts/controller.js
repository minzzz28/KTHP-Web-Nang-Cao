const { asyncHandler } = require('../../utils/asyncHandler'); const { success, created } = require('../../utils/response'); const service = require('./service'); const create = asyncHandler(async (req, res) => created(res, { message: 'Đã tạo hợp đồng nháp', data: await service.create(req.user, req.body) })); const list = asyncHandler(async (req, res) => success(res, await service.list(req.user, req.query))); const show = asyncHandler(async (req, res) => success(res, { data: await service.getForUser(req.user, req.params.id) })); const transition = asyncHandler(async (req, res) => success(res, { message: 'Đã cập nhật trạng thái hợp đồng', data: await service.transition(req.user, req.params.id, req.body) })); module.exports = { create, list, show, transition };
