const { asyncHandler } = require('../../utils/asyncHandler'); const { success } = require('../../utils/response'); const service = require('./service'); const dashboard = asyncHandler(async (req, res) => success(res, { data: await service.dashboard(req.user) })); const users = asyncHandler(async (req, res) => success(res, await service.listUsers(req.user, req.query))); const userStatus = asyncHandler(async (req, res) => success(res, { message: 'Đã cập nhật trạng thái tài khoản', data: await service.setUserStatus(req.user, req.params.id, req.body) })); const roomStatus = asyncHandler(async (req, res) => success(res, { message: 'Đã cập nhật trạng thái phòng', data: await service.setRoomStatus(req.user, req.params.id, req.body) })); const propertyVerification = asyncHandler(async (req, res) => success(res, { message: 'Đã cập nhật xác minh khu trọ', data: await service.setPropertyVerification(req.user, req.params.id, req.body) })); module.exports = { dashboard, users, userStatus, roomStatus, propertyVerification };
